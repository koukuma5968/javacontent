java.util.concurrent.ScheduledExecutorServiceインターフェースで、
1秒後にスレッドを実行するメソッドとして、正しいのもを選びなさい。（1つ選択）

A
se.schedule(() -> {
	System.out.println("schedule");
}, 1, TimeUnit.SECONDS);

B
se.schedule(() -> {
	System.out.println("schedule");
}, TimeUnit.MILLISECONDS);

C
se.execute(() -> {
	System.out.println("schedule");
}, 1, TimeUnit.SECONDS);

D
se.execute(() -> {
	System.out.println("schedule");
});


A

