抽象クラスの説明として、正しいものを選びなさい。（3つ選択）

A
サブクラスに継承することは出来ない。

B
実体を持つメソッドを1つ以上持つ必要がある。

C
クラスに「abstract」キーワードを付けて定義する。

D
抽象クラスのインスタンスを生成することは出来ない。

E
「implements」キーワードを使って実装することが出来る。

F
抽象クラスの持つ抽象メソッドはサブクラスで実装する必要がある。

C,D,F
